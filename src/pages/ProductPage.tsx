import React from 'react';

export const ProductPage = ({productId}: {productId: string} ) => {
    return (
        <main>
            <h1>page product id - {productId}</h1>
        </main>
    );
};

